services:
  - type: web
    name: supplymind-response-engine
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn backend.response_engine.main:app --host 0.0.0.0 --port 10000
    runtime: python-3.11.8